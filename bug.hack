function foo(x: int): int {
  if (x == 0) {
    return 1;
  } else {
    return x * foo(x - 1);
  }
}

function main(): void {
  echo foo(5);
}

This code will cause a stack overflow error if a sufficiently large number is passed to the `foo` function.  The recursive calls to `foo` continue until the call stack is exhausted.